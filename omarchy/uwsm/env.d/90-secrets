# Changes require a restart to take effect.

# Sourced by uwsm at session start, so GUI apps and the agents they spawn
# inherit these. ~/.bashrc only covers interactive shells.

# Local secrets (untracked)
[ -f "$HOME/.secrets" ] && . "$HOME/.secrets"

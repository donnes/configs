# packages-debian.txt - Core machine dependencies
# Edit to customize
# Install with: sudo apt-get install -y $(grep -v '^#' packages-debian.txt)

# Core dev tools
git
neovim
ripgrep
fd-find
fzf
build-essential
curl
unzip

# Shell enhancements
zoxide
